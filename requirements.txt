# Core scientific stack
numpy>=1.26
pandas>=2.2
pyarrow>=14.0
scikit-learn>=1.4
scipy>=1.12
tqdm>=4.66
pyyaml>=6.0

# Pool-feature language detection (optional but recommended; pipeline falls
# back to an ASCII heuristic if missing).
langdetect>=1.0.9

# Plotting
matplotlib>=3.8

# PyTorch (A100 / Vertex AI Workbench typically already has CUDA torch
# pre-installed; this version constraint is compatible with CUDA 12.x bf16).
torch>=2.2

# Hugging Face stack
transformers>=4.41
accelerate>=0.30
datasets>=2.19
huggingface_hub>=0.23
safetensors>=0.4
sentencepiece>=0.2
tokenizers>=0.19

# Optional: Flash Attention 2 (large encoder speedup on A100/H100). The
# embedder gracefully falls back to SDPA when this wheel fails to install
# (no nvcc, mismatched CUDA, T4/L4 etc.), so this line is best-effort.
flash-attn>=2.5.0 ; platform_system == "Linux"

# Optional: FAISS for fast nearest-neighbor lookup over the shipped
# training-item cache. If the wheel cannot install, the runtime falls back
# to a numpy brute-force lookup (~50ms for 200k x 256-dim) automatically.
faiss-cpu>=1.7.4

# Optional Google Cloud secret access (used only if HF_TOKEN env var is absent)
google-cloud-secret-manager>=2.20 ; python_version >= "3.9"

# Optional notebook interactivity
ipywidgets>=8.1

# Optional GCS sync of artifacts
google-cloud-storage>=2.16
